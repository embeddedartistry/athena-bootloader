#include <avr/eeprom.h>

// The definitions are shared between the NetEEPROM library and this bootloader.
// They are kept in the library folder to work with the Arduino SDK properly
// The library path is used as an include path in the Makefile
#include <NetEEPROM/NetEEPROM_defs.h>
